# Home

This is the homepage. With `jekyll-theme-primer`, every Markdown file is
rendered with a clean, responsive, GitHub-style default. Write pages and posts
in Markdown; you never need to touch HTML or CSS.
